spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://dpg-d8l1rkjeo5us73b072tg-a.oregon-postgres.render.com:5432/employeeerp?sslmode=require}
    username: ${DB_USERNAME:employeeerp_user}
    password: ${DB_PASSWORD:ZRjTnvm14MWYPh0JQawblLenFpG6zpLp}
    driver-class-name: org.postgresql.Driver

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

app:
  mail:
    from: ${MAIL_FROM:${MAIL_USERNAME:ncodedsystems@gmail.com}}
    display-name: ${MAIL_DISPLAY_NAME:Ncoded Systems}

server:
  port: ${PORT:8080}
